using UnityEngine;
using System.Collections;

//[ExecuteInEditMode]
public class BlockController : MonoBehaviour {

	void Start () {
//		GetComponent<MeshRenderer>().sharedMaterial.mainTextureScale = new Vector2 (transform.localScale.x, -transform.localScale.y) * 2f;
	}

}
